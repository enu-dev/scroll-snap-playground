# 引き継ぎパッケージ：scroll-snap Playground

## プロジェクト基本情報

| 項目 | 内容 |
|------|------|
| プロジェクト名 | scroll-snap Playground |
| リポジトリ名 | `scroll-snap-playground` |
| 制作期間 | 1日 |
| GitHub Pages URL | （えぬ公開後に記入） |
| リポジトリURL | （えぬ公開後に記入） |
| QA完了日 | 2026-05-18（事前チェック完了。実機テストはえぬが起動時に実施） |

---

## 開発の目的・解決した課題

CSS の `scroll-snap-*` プロパティ群は便利だが、**プロパティが多くてどれが何をするか把握しにくい**。さらに既存の体感ツールがほぼなく、CodePenで自前で書いて試すしかなかった。

このツールは「コントロールを切り替えながら、実際にコンテナをスクロールして挙動を確かめられる」体験を中心に設計。横スクロール（カルーセル）・縦スクロール（フルスクリーン）両対応で、scroll-snap-* のすべての主要プロパティを1画面で試せる。

教育コンテンツとしても、実用ツールとしても使える二面性が特徴。

---

## 技術的ハイライト（バズに使える要素）

1. **scroll-snap の主要5プロパティ全部を1画面で試せる**：type / align / stop / padding / margin
2. **横スクロール / 縦スクロール 切替**：1ツールで2軸カバー
3. **プリセット3種**（カルーセル / フルスクリーン / フォトギャラリー）でゼロから考えなくていい
4. **方向ミスマッチ警告**：scroll-snap-type の方向（x/y）とコンテナのスクロール方向が合ってないとスナップが効かない件を警告メッセージで明示
5. **クリアボタン + プリセット toggle 解除 + 手動操作で自動解除**（前作の反省を最初から設計に反映）
6. **HTML 100行 / CSS 320行 / JS 160行**（合計580行）の完全バニラ
7. **依存ライブラリゼロ・CDN読み込みなし**

---

## 詰まったこと・失敗したこと（コンテンツに使える素材）

1. **scroll-snap-type の方向（x/y）とコンテナのスクロール方向のミスマッチ問題**
   - ユーザーが「縦スクロール」を選んでるのに type を `x mandatory` のままにしておくとスナップが全く効かない
   - 最初は無言で何も起きなくて「バグった」と思ったが、仕様的に正しい
   - 結局、警告メッセージを明示する設計にした

2. **scroll-snap-stop: always の体感がぱっと分からない**
   - ドキュメント上は「一気にスクロールしても飛び越えない」だが、実際に試さないと分からない
   - フルスクリーンプリセットで always にしておくことで「あ、これか」と理解できる構成にした

3. **子要素数を可変にする実装で innerHTML を毎回書き換える設計に**
   - 子要素数スライダー動かすたびに DOM 再生成
   - パフォーマンス的にイマイチだが、子要素数3〜10程度なら気にならないレベル
   - もっと大きい数字なら差分更新が必要だが、scroll-snap 体感ツールとしては不要

4. **クリアボタンを最初から設計に入れた**
   - 前作 clamp() Generator で動作確認時に指摘されたフィードバックを反映
   - 「ステートを持つUIには必ず解除手段を入れる」という基本品質を初手から組み込んだ

---

## スクリーンショット見せ場シナリオ

**シーン1（最重要・GIF推奨）：**
- プリセット `カルーセル` をクリック → 横スクロール状態
- プレビューエリアを右にスクロール → スムーズに中央でスナップする様子
- 続いて type を `x proximity` に切り替えて再度スクロール → 中途半端な位置でも止まる様子の対比

**シーン2：**
- 「スクロール方向」を「縦スクロール」に切替 + プリセット `フルスクリーン`
- プレビューエリアを下にスクロール → 1画面ずつスナップする「フルスクリーンLP」感

**シーン3：**
- scroll-snap-type が `x` のまま、方向を「縦」に切替
- 警告メッセージ「⚠ scroll-snap-type の方向（x/y）が、コンテナのスクロール方向と一致していません」が表示される様子
- → 「気づきにくい罠を教えてくれる」教育性のアピール

---

## えぬ向けのGitHub Pages公開手順書

1. GitHub で新しいリポジトリを作成
   - リポジトリ名：`scroll-snap-playground`
   - 公開設定：Public

2. ファイル一式をアップロード
   - `index.html`
   - `css/style.css`
   - `js/script.js`
   - `README.md`
   - `LICENSE`

3. GitHub Pages 有効化
   - Settings → Pages → Source: "Deploy from a branch"
   - Branch: `main` / `(root)` → Save

4. 数分後、`https://[えぬのGitHubユーザー名].github.io/scroll-snap-playground/` で公開

5. README.md の「デモURL」欄を上記URLに更新

6. リポジトリ About 設定
   - Description：「CSS scroll-snap-* を実際にスクロールしながら挙動を確かめられるプレイグラウンド。横/縦両対応、依存ゼロ。」
   - Website：GitHub Pages URL

7. Topics タグ：`javascript` / `html-css-javascript` / `vanilla-js` / `claude-code` / `css` / `scroll-snap` / `playground`

8. 公開URL・リポジトリURLをCEOに共有

---

## 投稿用アセット設計指示書（宮崎UIUXより）

### アセット1：カルーセルGIF（リリース投稿に必須・最重要）
- 形式：GIF
- 尺：5〜7秒
- 撮影手順：
  1. プリセット `カルーセル` をクリック
  2. プレビューエリアを右にゆっくりスクロール → 中央でスナップする様子
  3. CleanShot X / ScreenToGif で 600px幅程度に圧縮

### アセット2：mandatory vs proximity 比較GIF
- 形式：GIF
- 尺：8〜10秒
- 撮影手順：
  1. type を `x mandatory` でスクロール（中央にきっちり止まる）
  2. type を `x proximity` に切替 → 同じスクロール（中途半端な位置でも止まる）
  3. 「2つの違いが目で分かる」のがポイント

### アセット3〜7：制作日誌5本分の素材

---

## 制作中ポストストック（5本）

> Phase 1ルール準拠、一人称「自分」、組織用語なし。
> ボツ作品ガード適用。

### Stock-1：制作日誌-1 設計
```
scroll-snap のツール作るとき、
まず「既存ツールあるか」探したらほぼ何もなかった。
CodePen 開いて自分で書いて試す世界。
これは作る価値がある、と思った。
横/縦両対応にして、全プロパティ1画面で試せるやつ。
```

### Stock-2：制作日誌-2 開発初期
```
scroll-snap-type の方向（x/y）と
コンテナのスクロール方向がミスマッチだと
スナップが全く効かない、って仕様、
最初は「バグった？」と思った。
仕様的には正しいけど、気づきにくい。
ツールに警告メッセージ出すようにした。
```

### Stock-3：制作日誌-3 開発中期
```
scroll-snap-stop: always、
ドキュメント上の「一気にスクロールしても飛び越えない」って説明、
読んでも頭で再現できなかった。
実際に試して初めて「あ、これか」と分かった。
こういうのは絶対に体感ツールが必要。
```

### Stock-4：制作日誌-4 開発後期
```
子要素数を可変にするのに DOM 毎回再生成してる。
3〜10個なら全然気にならないけど、
もっと大きい数字なら差分更新必要。
scroll-snap 体感ツールには過剰だな、と判断して
シンプル実装で落ち着いた。
過剰最適化したくなる癖、毎回戦ってる。
```

### Stock-5：制作日誌-5 リリース直前
```
クリアボタン・プリセット toggle 解除・手動操作で自動解除、
今回は最初から設計に入れた。
前作で動作確認中に指摘されたフィードバックを反映。
「ステートを持つUIには必ず解除手段を入れる」、
1回失敗すると次から忘れない。
```

---

## 広報チームへのメモ（PM神田より）

- **バズ筋：** mandatory vs proximity 比較GIFが教育コンテンツとして強い。エンジニアの「あ、違いが分かった」リアクションを狙う
- **差別化軸：** scroll-snap 体感ツールは既存ほぼなし。「ニッチだけど刺さる人には深く刺さる」タイプ
- **投稿構造案：** GIF + 「scroll-snap、ドキュメント読んでも掴めないやつ目で試せる」フック
- **連投候補：** 制作日誌 Stock-1 〜 Stock-5 をリリース後に連投

---

## 成果物ファイル一覧

- [x] `index.html`
- [x] `css/style.css`
- [x] `js/script.js`
- [x] `README.md`
- [x] `LICENSE`（MIT）
- [ ] `assets/`（不要）

---

## CEO承認待ち：広報フェーズへの引き継ぎ

引き継ぎ条件チェック：
- [x] QA事前チェックリスト作成済み
- [x] README.md 完備
- [x] LICENSE 完備
- [x] 個人情報・APIキー混入なし
- [x] 制作日誌5本ストック作成済み
- [x] 前作フィードバック（クリア機能等）反映済み

→ CEO 高橋 承認 → えぬへ引き継ぎ → GitHub Pages 公開 → URL共有 → 広報チーム着手
